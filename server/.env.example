# Database
MONGODB_URI=mongodb://localhost:27017/testbook

# Security
JWT_SECRET=dev_jwt_secret_key_12345
PORT=5000

# AWS S3 / Cloudflare R2 file uploads (Optional: falls back to local uploads/ directory if missing)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_S3_BUCKET_NAME=
AWS_REGION=us-east-1

# OpenAI API (Optional: uses simulated smart summary generator if missing)
OPENAI_API_KEY=

# Google Gemini API (Optional: uses simulated doubt solving tutor chatbot if missing)
GEMINI_API_KEY=
